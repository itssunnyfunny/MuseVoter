'use client'

import React from 'react'
import { signIn, signOut, useSession } from 'next-auth/react'

function AppBar() {
  return (
    <div>
        AppBar
        </div>
  )
}

export default AppBar